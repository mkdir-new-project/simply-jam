enum MessageTypes {
    USER_JOIN,
    USER_LEAVE,
    CONNECT,
    PING,
    PONG,
    GET_TRACK,
    NEW_TRACK,
    SEEK_TRACK,
    START_TRACK,
    PAUSE_TRACK,
    JOIN_ROOM,
    LEAVE_ROOM,
    CREATE_ROOM,
}

export default MessageTypes;