import React from "react";
import WsManager from "../../../structures/WsManager";

// Handles participant joined events

interface NavParticipantsProps {
    ws: WsManager;
}

export const NavParticipants: React.FC<NavParticipantsProps> = ({ ws }) => {
    return (<></>)
}